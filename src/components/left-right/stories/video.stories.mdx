import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks';
import { LeftRight } from '../Left-Right';
import { argTypes } from '../Left-Right.argTypes';

<Meta
    title="Components/Left Right/Video"
    component={ LeftRight }
    args={ LeftRight.defaultProps }
    argTypes={ argTypes }
/>

# Embedded Video

Placeholder

## 1. Default

### No Crop (Start 16:9)

<Story
    name="Default"
    args={{
        type: 'video',
    }}>
    {(args) => <LeftRight {...args} />}
</Story>

### Auto Crop

<LeftRight
    {...LeftRight.defaultProps}
    type="video"
    crop="auto"
    />

### Fixed Crop

<LeftRight
    {...LeftRight.defaultProps}
    type="video"
    crop="fixed"
    />

## 2. Full Width Bleed

### No Crop (Start 16:9)

<Story
    name="Full Width"
    args={{
        type: 'video',
        isFullWidth: true,
    }}>
    {(args) => <LeftRight {...args} />}
</Story>

### Auto Crop

<LeftRight
    {...LeftRight.defaultProps}
    type="video"
    isFullWidth={true}
    crop="auto"
    />

### Fixed Crop

<LeftRight
    {...LeftRight.defaultProps}
    type="video"
    isFullWidth={true}
    crop="fixed"
    />

## 3. Rounded Corners

<Story
    name="Rounded Corners"
    args={{
        type: "video",
        crop: "auto",
        hasRoundedCorners: true
    }}>
    {(args) => <LeftRight {...args} />}
</Story>

## 4. Reversed

<Story
    name="Reversed"
    args={{
        type: "video",
        crop: "auto",
        isReverse: true
    }}>
    {(args) => <LeftRight {...args} />}
</Story>

# Props

<ArgsTable of={LeftRight} />