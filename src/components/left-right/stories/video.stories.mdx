import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks';
import { LeftRight } from '../Left-Right';
import { argTypes } from '../Left-Right.argTypes';

<Meta
    title="Components/Left Right/Video"
    component={ LeftRight }
    args={ LeftRight.defaultProps }
    argTypes={ argTypes }
/>

# Embedded Video

Placeholder

## 1. Default

### No Crop

<Story
    name="Default (No Crop)"
    args={{
        type: 'video',
    }}>
    {(args) => <LeftRight {...args} />}
</Story>

### Auto Crop

<Story
    name="Default (Auto Crop)"
    args={{
        type: 'video',
        crop: 'auto'
    }}>
    {(args) => <LeftRight {...args} />}
</Story>

### Fixed Crop

<Story
    name="Default (Fixed Crop)"
    args={{
        type: 'video',
        crop: 'fixed'
    }}>
    {(args) => <LeftRight {...args} />}
</Story>

## 2. Full Width Bleed

### No Crop

<Story
    name="Full Width (No Crop)"
    args={{
        type: 'video',
        isFullWidth: true,
    }}>
    {(args) => <LeftRight {...args} />}
</Story>

### Auto Crop

<Story
    name="Full Width (Auto Crop)"
    args={{
        type: 'video',
        isFullWidth: true,
        crop: 'auto'
    }}>
    {(args) => <LeftRight {...args} />}
</Story>

### Fixed Crop

<Story
    name="Full Width (Fixed Crop)"
    args={{
        type: 'video',
        isFullWidth: true,
        crop: 'fixed'
    }}>
    {(args) => <LeftRight {...args} />}
</Story>

# Props

<ArgsTable of={LeftRight} />