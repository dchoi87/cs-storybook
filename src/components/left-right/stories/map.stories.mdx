import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks';
import { LeftRight } from '../Left-Right';
import { argTypes } from '../Left-Right.argTypes';

<Meta
    title="Components/Left Right/Map"
    component={ LeftRight }
    args={ LeftRight.defaultProps }
    argTypes={ argTypes }
/>

# Embedded Map

Placeholder

## 1. Default

### No Crop (Start 16:9)

<Story
    name="Default"
    args={{
        type: 'map',
    }}>
    {(args) => <LeftRight {...args} />}
</Story>

### Auto Crop

<LeftRight
    {...LeftRight.defaultProps}
    type="map"
    crop="auto"
    />

### Fixed Crop

<LeftRight
    {...LeftRight.defaultProps}
    type="map"
    crop="fixed"
    />

## 2. Full Width Bleed

### No Crop (Start 16:9)

<Story
    name="Full Width"
    args={{
        type: 'map',
        isFullWidth: true,
    }}>
    {(args) => <LeftRight {...args} />}
</Story>

### Auto Crop

<LeftRight
    {...LeftRight.defaultProps}
    type="map"
    isFullWidth={true}
    crop="auto"
    />

### Fixed Crop

<LeftRight
    {...LeftRight.defaultProps}
    type="map"
    isFullWidth={true}
    crop="fixed"
    />

## 3. Rounded Corners

<Story
    name="Rounded Corners"
    args={{
        type: "map",
        crop: "auto",
        hasRoundedCorners: true
    }}>
    {(args) => <LeftRight {...args} />}
</Story>

## 4. Reversed

<Story
    name="Reversed"
    args={{
        type: "map",
        crop: "auto",
        isReverse: true
    }}>
    {(args) => <LeftRight {...args} />}
</Story>

# Props

<ArgsTable of={LeftRight} />